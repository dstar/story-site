<h1>New chapter</h1>

<%= form_for @chapter, :action => "/stories/create_chapter/#{@story.id}" do -%>
  <%= error_messages_for :chapter %>
  <%= partial 'new_chapter_form' %>
  <%= submit_tag "Create Chapter" %>
<% end =%>

<%= link_to 'Back', "/stories/show/#{@story.id}" %>
