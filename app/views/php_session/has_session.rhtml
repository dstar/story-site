<div class="userinfo">
	<%= render :partial => "logged_in" if @authinfo[:user] and @authinfo[:user] != "Anonymous"%>
	<%= render :partial => "login" unless @authinfo[:user] and @authinfo[:user] != "Anonymous" %>
</div>
