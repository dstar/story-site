<div class="userinfo">
	<%= render :partial => "logged_in" if @authinfo[:username] and @authinfo[:username] != "Anonymous"%>
	<%= render :partial => "login" unless @authinfo[:username] and @authinfo[:username] != "Anonymous" %>
</div>
