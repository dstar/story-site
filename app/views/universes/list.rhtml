<h1>Universes</h1>

<%= render :partial => "universe_wstories", :collection => @universes %>

<br />

<% logger.debug("Done with univers_wstories calls") %>

<% if @authinfo[:user] and @authinfo[:user].has_site_permission('admin') %>
<%= link_to 'New universe', :controller => 'site', :action => 'new_universe' %>
<% end %>